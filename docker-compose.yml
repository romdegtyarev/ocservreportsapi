version: '3.8'

services:
  ocservreports:
    build: .
    container_name: ocservreports
    restart: always
    volumes:
      - ${W_DIR}:/data/
    environment:
      - TOKEN=${TG_TOKEN}
      - CHAT_ID=${TG_CHAT_ID}
      - DIRECTORY=/data

